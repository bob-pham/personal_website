{
  "name": "personal_website",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo\"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "github-calendar": "^2.2.9",
    "three": "^0.128.0"
  },
  "files": ["./images", "./blender_assets"]
}
